[project]
name = "dynamo-db-workshop"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "python-dotenv==1.1.1",
    "ruff>=0.12.12",
]
